#lang setup/infotab

(define name "mirrors")

(define blurb 
  '("Scheme syntaxes for quickly creating composable blocks of XML and Javascript."))

(define release-notes
  '("Initial release."))

(define primary-file
  "mirrors.ss")

(define url "http://svn.untyped.com/mirrors/")

(define doc.txt "doc.txt")

(define scribblings '(("doc/mirrors.scrbl" (multi-page))))

(define categories '(devtools net ui xml))

(define required-core-version "3.99")

