(module info (lib "infotab.ss" "setup")
  (define name "list")
  (define blurb
    (list "Combines the best of the standard mzscheme list libraries."))
  (define primary-file "list.ss")
  (define doc.txt "doc.txt")
  (define categories '(datastructures))
  (define version "2")
  (define required-core-version "3.99"))
